# Temas para preparar la alabanza — Culto de Alabanza

**Ubicación en la app:** Instrucciones → Alabanza → **Temas alabanza** (`rol = temas_alabanza`).

Estos seis temas son los que se **eligen por culto** en el detalle (selector «Tema de la introducción»), no la guía del rol de introducción.

---

Contenido oficial migrado desde la antigua fila `introduccion` (ver `supabase/migrations/20260525150000_instrucciones_temas_alabanza.sql`).

Para editar: actualizar `contenido_es` / `contenido_ca` en `instrucciones_culto` donde `rol = 'temas_alabanza'` y tipo Alabanza.

Referencia histórica del texto: `alabanza-introduccion.md` (documento base antes del cambio de rol).
